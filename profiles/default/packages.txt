# GLB profile: default
#
# One package per line. Blank lines and lines starting with '#' are
# ignored; trailing '# comment' after a package name is stripped too.
#
# Package names are passed as-is to your distro's package manager
# (apt/dnf/pacman/zypper), so stick to names that are the same across
# all of them where you can. If a name differs per distro, you'll need
# separate profiles or handle that package manually for now.

git
zsh
tmux
neovim
curl
ripgrep
fzf
